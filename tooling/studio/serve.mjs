import { createServer } from 'node:http';import { readFile, stat } from 'node:fs/promises';import { extname, resolve, sep } from 'node:path';
const root=resolve(import.meta.dirname,'../../dist/studio'),host='127.0.0.1',port=Number(process.env.STUDIO_PORT||4174),mime=new Map([['.html','text/html; charset=utf-8'],['.css','text/css; charset=utf-8'],['.js','text/javascript; charset=utf-8'],['.mjs','text/javascript; charset=utf-8'],['.json','application/json; charset=utf-8'],['.txt','text/plain; charset=utf-8']]),contained=(path)=>path===root||path.startsWith(root+sep);
const server=createServer(async(request,response)=>{try{const url=new URL(request.url||'/','http://'+host+':'+port),pathname=decodeURIComponent(url.pathname==='/'?'/index.html':url.pathname);let file=resolve(root,'.'+pathname);if(!contained(file))return response.writeHead(403).end('Forbidden');const info=await stat(file);if(info.isDirectory())file=resolve(file,'index.html');if(!contained(file)||!(await stat(file)).isFile())throw new Error('not-file');const body=await readFile(file);response.writeHead(200,{'content-type':mime.get(extname(file))||'application/octet-stream','cache-control':'no-store','x-content-type-options':'nosniff','referrer-policy':'no-referrer'});response.end(body)}catch{response.writeHead(404,{'content-type':'text/plain; charset=utf-8'});response.end('Not found')}});server.listen(port,host,()=>console.log('Serving NeoSmartUI Studio at http://'+host+':'+port));
